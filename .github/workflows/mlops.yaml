name: Full-MLOps-Pipeline

on:
  push:
    branches: [ main ]
    paths:
      - 'data/**'
      - 'src/**'
      - 'params.yaml'
      - '.github/workflows/mlops.yaml'

jobs:
  train-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install All Dependencies
        run: |
          python -m pip install --upgrade pip
          # We include kaggle just to prevent the ModuleNotFoundError 
          # even if we plan to skip the step.
          pip install dvc dvc-s3 xgboost pandas scikit-learn matplotlib plotly pyyaml kaggle

      - name: Configure DVC Remote
        env:
          dvc_token: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          dvc remote add origin https://dagshub.com/SAIF-BT/Predictive_maintenance.dvc || true
          dvc remote modify origin --local auth basic
          dvc remote modify origin --local user SAIF-BT
          dvc remote modify origin --local password $dvc_token

      - name: Pull Data from DagsHub
        run: |
          # This step is key: it puts the data in place so 'repro' doesn't have to 'ingest'
          dvc pull -r origin

      - name: Execute Pipeline (dvc repro)
        run: |
          # 'dvc repro train' tells the system: "I only care about the training results."
          # If the pulled data matches the lock file, it skips 'ingest' entirely.
          dvc repro train

      - name: Push Updated Model & Metrics
        env:
          dvc_token: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          dvc push -r origin
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add dvc.lock models/metrics.json || echo "No changes to lock/metrics"
          git commit -m "🚀 Automated Model Update [skip ci]" || echo "No changes to commit"
          git push origin main